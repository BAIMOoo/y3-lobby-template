# 多人副本队伍流程完善测试报告

> 本报告中的 PT-03 结论已被 2026-07-23 17:05 的复测日志推翻，请以 `多人副本队伍流程完善测试报告-2026年7月23日17时05分.md` 为准。

> 测试时间：2026-07-23 16:22-16:50
> 关联测试案：`增量测试案.md`
> 测试环境：Y3 编辑器调试客户端，单账号 `AID=5209342111`，BOB pre 服务

## 结论

本次修改通过 Lua 回归、语法检查和单客户端运行时验证。非队长权限、队长操作、目标成员校验与副本 RPC 参数均由双关卡回归测试覆盖；大厅和副本 UUID 的实际切换、按钮显隐、创建队伍、离开队伍、解散队伍已连接真实 BOB 服务验证。

当前工具仅连接一个游戏客户端，无法组成真实双人队伍，因此“第二个真实账号以非队长身份点击”和“成功转移/移除真实成员”未做双客户端端到端复现。这些分支已由双关卡 Lua 回归锁定，仍列为剩余人工联机验证项。

## 测试结果

| ID | 结果 | 验证证据 |
|---|---|---|
| PT-01 | 通过（逻辑回归） | `dungeon_team_permissions_test.lua` 在 `EntryMap`、`MapName001` 均验证非队长不会发送副本 RPC；RPC 边界再次校验队长。 |
| PT-02 | 通过（逻辑回归） | 双关卡回归验证队长、2 名成员时发送私人副本请求，并携带固定版本 `2.0`。真实双账号整队切换未执行。 |
| PT-03 | 通过（运行时） | 副本 UUID `25e6448f-7e73-11f1-88ae-03dc5a85955c` 中实际显示“返回初始关卡”；调用后重新连接到大厅 UUID `81ad7554-7e6b-11f1-8f5c-c78cd393ba6e`。 |
| PT-04 | 通过（逻辑回归） | 验证仅队长可转移，目标必须为队内其他成员；真实第二成员成功回包未执行。 |
| PT-05 | 通过（运行时） | 真实创建队伍 `101369` 后调用 `Team_LeaveTeam`，收到离队通知与成功回包。 |
| PT-06 | 通过（逻辑回归） | 验证仅队长可移除队内其他成员，非队长与自我移除均被拒绝；真实第二成员成功回包未执行。 |
| PT-07 | 通过（运行时 + 逻辑回归） | 真实 `Team_DismissTeam` 成功；回归测试验证非队长请求不会发出。 |
| PT-S1 | 通过 | 9 个相关 Lua 文件逐文件执行 `luac -p`，全部退出码 0；Y3 诊断无阻断错误。 |
| PT-S2 | 通过 | 8/8 Lua 测试通过。 |
| PT-S3 | 通过 | 大厅与副本均可启动，BOB 登录成功；未发现本次新增 Trace/Error。 |

## 回归测试清单

| 测试文件 | 结果 |
|---|---|
| `tests/bob_chat_echo_test.lua` | 通过 |
| `tests/bob_version_diagnostics_test.lua` | 通过 |
| `tests/dungeon_player_version_test.lua` | 通过 |
| `tests/dungeon_team_permissions_test.lua` | 通过 |
| `tests/dungeon_unit_spawn_test.lua` | 通过 |
| `tests/mapname001_event_diagnostics_test.lua` | 通过 |
| `tests/runtime_token_test.lua` | 通过 |
| `tests/world_chat_protocol_test.lua` | 通过 |

## 静态诊断

- `luac -p`：相关业务与测试文件全部通过。
- Y3 Lua 诊断：无新增阻断错误。
- 已存在告警：双关卡加载相同类型声明导致的重复定义，以及旧协议字段告警；本次不扩散处理。
- `git diff --check`：本次编辑的 Lua 与测试文件无空白错误；工作区其他 Y3 编辑器生成文件仍有既存尾随空格。

## 运行时证据

- 大厅真实登录：`map_id=81ad7554-7e6b-11f1-8f5c-c78cd393ba6e`。
- 副本识别：当前关卡为副本 UUID、调试模式号为 `1001` 时，`MatchTestIsBattleContext()` 返回 `true`。
- 返回按钮截图：`D:\y3test\y3\games\2.0\game\LocalData\mcp_screenshots\screenshot.png`。
- 返回流程：副本调用 `MatchTestReturnLobby()` 后，引擎进入切关状态；重新连接后当前关卡为大厅 UUID。
- 队伍流程：真实创建队伍返回 `member_limit=2`；离开队伍与解散队伍均收到成功回包。

## 剩余风险

- 需要两台真实客户端补测非队长实际点击、成功转移队长、成功移除成员以及整队进入副本。
- `MatchTestLocalPrivate()` 的既有 `KeyError: 'map_data'` 与本次修改无关，已记录在 Lua Trace 错题本。

## 交叉校验结果

| 项目 | 值 |
|---|---|
| 计划测试点数 | 10 |
| 报告测试点数 | 10 |
| 缺失 | 0 |
| 多余 | 0 |
| 校验结果 | 通过 |
