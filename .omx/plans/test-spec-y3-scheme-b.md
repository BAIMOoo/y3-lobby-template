# 方案 B：验证规格

## 静态验证

- `BobTestUI.json` 可解析，画板名正确，布局来自 `html_to_y3_ui.py`。
- `gen_ui_tree.py` 成功生成 `ui_tree/BobTestUI_Tree.json`。
- Lua 诊断无新增问题。
- 不存在“邀请队友”、血条、技能栏或战斗目标等越界内容。
- `runtime` 关键字段保持：状态、队伍、匹配、副本、聊天、返回、退出确认。

## 自动化回归

- 现有匹配按钮状态测试通过。
- 现有聊天回显测试通过。
- 现有退出清理测试通过。
- 新增验证聊天最多显示 5 条。
- 新增验证大厅与战斗聊天由同一个构建函数创建。

## 运行时验证

- 大厅：信息完整，成员操作与副本操作可见，聊天可发送，中央区域保持净空。
- 战斗：大厅面板隐藏；左上状态、左下聊天、返回与退出可见；无无关 HUD。
- 战斗：默认 `GameHUD` 隐藏；返回大厅后恢复显示。
- 退出：遮罩默认隐藏，打开后阻断背景操作，取消关闭，确认调用退出流程。
- 聊天：大厅和战斗结构与视觉一致，队伍/世界发送成功后清空输入框并展示反馈。

## 编辑器流程

- UI JSON 修改后执行 `hotfix_ui_editor`。
- 等待至少 3 秒后执行 `save_editor`。
- 启动或快速重启游戏，检查日志并截取 1920×1080 运行截图。
